# Keep Conventional Commits soft-check if available; otherwise skip.
if command -v npx >/dev/null 2>&1 && [ -f node_modules/@jannajs/lint/package.json ]; then
  npx @jannajs/lint verify-commit "$1"
fi
